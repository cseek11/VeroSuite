# VeroField LLM Rules (Two-Brain Model)
# Brain B (Implementation Agent)

# ⚠️ PHASE 2: Two-Brain Model Active
# This file replaces the old .cursorrules for Two-Brain operation

# 🚨 MANDATORY PRE-FLIGHT CHECK 🚨
# BEFORE ANY TASK: Read .cursor/enforcement/ENFORCEMENT_BLOCK.md
# IF FILE EXISTS: You are BLOCKED - STOP immediately and display the message
# IF FILE DOES NOT EXIST: Continue normally

# Core interface
@.cursor/rules/00-llm-interface.mdc

# Security essentials
@.cursor/rules/01-llm-security-lite.mdc

# Fix mode protocol (loaded when [FOLLOW_ENFORCER_REPORT] tag detected)
@.cursor/rules/02-llm-fix-mode.mdc

# Memory Bank summary (when available)
@.cursor/memory-bank/summary.md

# That's it. No other rules loaded by default.
# The auto-enforcer (Brain A) owns the full rule system.
