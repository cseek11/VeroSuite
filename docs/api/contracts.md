See `docs/migration/api-contracts.md` for the current endpoint catalog baseline (271 mapped routes from Nest startup logs). This file tracks the same content for Phase 0.5; future phases should update both locations or replace this reference with the maintained source of truth.

