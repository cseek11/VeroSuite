---
# Cursor Rules Index
# This file defines the order and priority of rule files for Hybrid Rule System v2.0

version: 2.0
project: VeroField
last_updated: 2025-11-21

# Rule files are loaded in order (top to bottom)
# Higher priority rules should be listed first
# All .mdc files in .cursor/rules/ are automatically loaded in alphabetical order

include:
  - .cursor/rules/00-master.mdc          # CRITICAL: Master rule with CI, REWARD_SCORE, pattern system
  - .cursor/rules/01-enforcement.mdc     # CRITICAL: Mandatory 5-step enforcement pipeline
  - .cursor/rules/02-core.mdc            # CRITICAL: Core philosophy, date handling, tech stack
  - .cursor/rules/03-security.mdc        # CRITICAL: Security rules: tenant isolation, RLS, auth
  - .cursor/rules/04-architecture.mdc    # CRITICAL: Monorepo structure, service boundaries
  - .cursor/rules/05-data.mdc            # CRITICAL: Data contracts, state machines, layer sync
  - .cursor/rules/06-error-resilience.mdc # CRITICAL: Error handling, no silent failures
  - .cursor/rules/07-observability.mdc   # CRITICAL: Structured logging, tracing, metrics
  - .cursor/rules/08-backend.mdc         # HIGH: Backend architecture (NestJS, Prisma)
  - .cursor/rules/09-frontend.mdc         # HIGH: Frontend architecture (React, React Native)
  - .cursor/rules/10-quality.mdc         # HIGH: Quality standards (testing, performance)
  - .cursor/rules/11-operations.mdc      # HIGH: CI/CD, workflows, reward score
  - .cursor/rules/12-tech-debt.mdc       # NORMAL: Tech debt logging, TODO/FIXME
  - .cursor/rules/13-ux-consistency.mdc # NORMAL: UI/UX coherence
  - .cursor/rules/14-verification.mdc     # NORMAL: Verification & testing standards

# Note: Cursor should auto-discover .mdc files, but this file ensures explicit loading order
# See .cursor/README.md for complete system documentation
