/**
 * Appointment entity in service-delivery.
 * TODO: define props: id, jobId, start, end, status, assignedTechnicianId, metadata.
 */
export class Appointment {
  constructor(/* props */) {
    // TODO
  }
}
