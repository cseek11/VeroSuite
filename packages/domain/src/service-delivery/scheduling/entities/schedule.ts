/**
 * Schedule entity for Service-Delivery bounded context.
 * TODO: define properties (tenantId, technicianId(s), dateRange, appointments, metadata).
 * Responsibility: represent a schedule aggregate root and invariants.
 */
export class Schedule {
  // TODO: fill props and invariants
  constructor(/* props */) {
    // TODO
  }
}
